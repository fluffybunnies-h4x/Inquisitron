<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <!-- Compile the real engine source, not a copy — tests must validate the code
       the app ships with. SuspicionRules.cs has no WPF dependency. -->
  <ItemGroup>
    <Compile Include="..\..\Services\SuspicionRules.cs" Link="SuspicionRules.cs" />
  </ItemGroup>

</Project>
